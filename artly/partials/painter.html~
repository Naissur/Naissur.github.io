<div class="painter-info">
	<h2>{{currentPainter.name}}</h2>
	<br>
	<img class="painter-info__img" src="{{currentPainter.img}}">
	<hr>
	<p class="painter-info__bio"> {{currentPainter.bio}} </p>
</div>

<div class="gallery">
	<h2>Gallery</h2>
	<div class="container">
		<img ng-repeat="picture in currentPainter.gallery" class="gallery__img" ng-src="{{picture}}">
	</div>
</div>
